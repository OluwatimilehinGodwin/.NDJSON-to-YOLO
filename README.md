# .NDJSON-to-YOLO
this script is to convert ultralytics .NDJSON file format  to YOLO so you can use in other platforms like roboflow.

note to fill in the proper URL for te script to work.
Also have a good internet access, cause the converter only tries 3 times to find the image in the URL.
